# Google Analytics 4 measurement ID (format: G-XXXXXXXXXX).
# Get it from Google Analytics → Admin → Data Streams → your web stream.
# Leave empty to disable analytics entirely (no script, no events).
NEXT_PUBLIC_GA_ID=
